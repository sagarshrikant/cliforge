/* ============================================================================
 * DEPRECATED LOCAL COPY — this file is now part of the cliforge SDK.
 *
 * Use:
 *   #include <cliforge/cf_argv_slice.h>
 *
 * Installed by cliforge's `make install` to:
 *   /usr/include/cliforge/cf_argv_slice.h
 *
 * This shim is kept only for backward-compatibility during migration.
 * ========================================================================= */
#include <cliforge/cf_argv_slice.h>
